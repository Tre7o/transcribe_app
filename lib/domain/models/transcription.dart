class Transcription {
  Transcription(this.fileName, this.currentDate, this.currentTime, this.transcribedText);

  final String fileName;
  final String currentDate;
  final String currentTime;
  final String? transcribedText;
}
